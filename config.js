/**
 * Default config for GitHub Pages deployment.
 * Local: copy config.example.js to config.js and add your credentials (config.js is gitignored).
 */
window.BANDFLOW_CONFIG = window.BANDFLOW_CONFIG || {};

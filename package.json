{
  "name": "retiflux",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "supabase": "^2.78.1"
  },
  "scripts": {
    "build": "node scripts/vercel-inject-config.mjs",
    "deploy": "vercel --prod",
    "supabase": "supabase",
    "db:push": "supabase db push",
    "db:reset": "supabase db reset"
  }
}
